#include<iostream>
#include<vector>
#include <bitset>

using namespace std;
bitset<33554432> s;
vector<int> order;

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int m;
	while (1) {
		int m;
		cin >> m;
		
		if (!s[m]) {
			s[m] = 1;
			cout << m << ' ';
		}
		if (cin.peek() == '\n') break;
	}

	return 0;
}
